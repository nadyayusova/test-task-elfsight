import styled from "styled-components";

const StyledSearch = styled.div`
  text-align: center;
`;

function Search() {
  return (
    <StyledSearch>
      search
    </StyledSearch>
  );
}

export default Search;
